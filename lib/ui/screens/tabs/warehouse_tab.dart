import 'package:flutter/material.dart';

class WarehouseTab extends StatelessWidget {
  const WarehouseTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Warehouse", style: TextStyle(fontSize: 22)),
    );
  }
}
