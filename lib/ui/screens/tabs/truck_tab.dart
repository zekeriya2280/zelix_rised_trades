import 'package:flutter/material.dart';

class TruckTab extends StatelessWidget {
  const TruckTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Truck Manager", style: TextStyle(fontSize: 22)),
    );
  }
}
